[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
[transfer]
    fsckobjects = true
[fetch]
    fsckobjects = true
    prune = true
    prunetags = true
    parallel = 0
[receive]
    fsckObjects = true
[pull]
    ff = only
    rebase = true
[merge]
    conflictStyle = zdiff3
    tool = meld
[color]
    ui = auto
[init]
    defaultBranch = main
[feature]
    manyFiles = true
[core]
    editor = emacs
	excludesfile = /Users/rory/.gitignore
    pager = delta
    autocrlf = input
	attributesfile = /Users/rory/.gitattributes
[gpg]
    format = ssh
[gpg "ssh"]
    program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
[commit]
    gpgsign = true
[push]
    default = simple
    autoSetupRemote = true
[log]
    date = iso
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true
[branch]
    sort = -committerdate
[pack]
    depth = 20
[include]
    path = ~/.gitconfig.local
[merge "mergiraf"]
    name = mergiraf
    driver = mergiraf merge --git %O %A %B -s %S -x %X -y %Y -p %P -l %L
